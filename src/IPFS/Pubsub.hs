module IPFS.Pubsub where

-- import qualified Data.ByteString.Lazy.Char8 as L8
-- import Network.HTTP.Simple
-- import IPFS

-- $ concat $ [endpoint, "api/pub?arg=", topic, "?arg=", message]

-- pub topic message = httpLBS $ parseRequest_ "http://localhost:5001/api/v0/api/pub?arg=chat?arg=helloworld"
